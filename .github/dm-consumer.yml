## Config for the org-ai-assisted/dist-ai reusable test workflow.
## The widget suite runs secure-terminal offscreen (PyQt6 + pyte) and the fuzz
## suite uses hypothesis, so the container needs these on top of python3.
dist-ai-tests:
  apt-packages: "python3 python3-pyqt6 python3-pyte python3-hypothesis"
